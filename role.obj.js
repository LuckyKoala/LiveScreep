var mod = function(name) {
    this.roleName = name;
    this.roleConfig = {
        inStack: [],
        outStack: [],
    };
    this.loop = function(creep) {
        //Get resource from fromStack
        //Use resource from toStack
    };
};

module.exports = mod;